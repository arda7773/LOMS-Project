plugins {
    id 'application'
}

dependencies {
    implementation(project(":core"))

    testImplementation platform('org.junit:junit-bom:5.11.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

application {
    mainClass = 'com.example.loms.Main'
}